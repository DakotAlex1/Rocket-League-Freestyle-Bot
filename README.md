# RLBotPython
a Python bot using the RLBot framework

It shows you how to:
- Install the RLBot GUI
- Use it to play against with!

## Changing the bot

- Bot behavior is controlled by `src/bot.py`
- Bot appearance is controlled by `src/appearance.cfg`
